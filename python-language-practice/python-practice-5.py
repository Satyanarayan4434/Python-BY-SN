def add(a, b):
    a = 6
    b = 7
    c = a + b
    print(c)
    
add(3, 2)

# Output: 13, because the values of a and b are changed inside the function, and the original values passed as arguments (3 and 2) are not used in the calculation.